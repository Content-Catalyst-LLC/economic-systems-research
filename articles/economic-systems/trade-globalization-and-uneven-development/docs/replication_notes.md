# Replication Notes

From the article folder:

```bash
make python
make r
make sql
make julia
```

Install Python requirements:

```bash
python3 -m pip install -r requirements.txt
```

Install R dependencies:

```r
source("r/packages.R")
```

Stata is optional and requires a licensed Stata installation.
